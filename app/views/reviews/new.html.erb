<h1>Add a new Review</h1>
<div class="container">
  <%= simple_form_for [@restaurant, @review]  do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit %>
  <% end %>
  <%= link_to "Back to the detail", restaurant_path(@restaurant) %>
</div>
